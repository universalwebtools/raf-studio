import { initializeApp } from "https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js";
import { getDatabase, ref, get, update } from "https://www.gstatic.com/firebasejs/12.2.1/firebase-database.js";
import { firebaseConfig, WEBSITE_ROOT } from "./firebase-config.js";
const app=initializeApp(firebaseConfig),db=getDatabase(app);const $=(s,r=document)=>r.querySelector(s);
const defaults={twoWorldsTitle:'DWA ŚWIATY.\nJEDEN STYL.',twoWorldsDesc:'Osobne działy fotografii i filmu, wspólna estetyka RAF.studio.',photoTitle:'FOTOGRAFIA',photoDesc:'Śluby, sesje, eventy, produkt i wizerunek.',photoButton:'Wejdź →',filmTitle:'FILM',filmDesc:'Reklama, eventy, social media, produkt i slow motion.',filmButton:'Wejdź →',aboutEyebrow:'O MNIE',aboutTitle:'CZŁOWIEK PO DRUGIEJ STRONIE KAMERY.',aboutDesc:'Tworzę zdjęcia i filmy dla ludzi oraz marek. Lubię czysty obraz, prawdziwe emocje, dobre światło i realizacje, które nie wyglądają jak gotowy szablon.',fact1Title:'FOTOGRAFIA',fact1Text:'Śluby • sesje • eventy • produkt',fact2Title:'FILM',fact2Text:'Reklama • social • event • slow motion',fact3Title:'RAF.studio',fact3Text:'Jedna spójna identyfikacja.',reviewsTitle:'OPINIE.',reviewsDesc:'Rekomendacje klientów.',brandsTitle:'MARKI.',brandsDesc:'Wybrane współprace.',contactEyebrow:'KONTAKT',contactTitle:'ZRÓBMY COŚ DOBREGO.',contactDesc:'Fotografia, film albo oba naraz.',showTwoWorlds:true,showAbout:true,showReviews:true,showBrands:true,showContact:true};let state={...defaults};
function input(k,label,area=false){return `<label>${label}${area?`<textarea data-k="${k}"></textarea>`:`<input data-k="${k}">`}</label>`}
function showToggle(key){return `<label style="display:flex;align-items:center;gap:8px;margin:10px 0 16px"><input type="checkbox" data-bool="${key}" style="width:auto"> Pokazuj tę sekcję</label>`}
function inject(){const mount=$('#homeSectionsMount');if(!mount)return;mount.innerHTML=`
<div class="adminCard"><div class="eyebrow">02 / DWA ŚWIATY</div><h3>Fotografia + Film</h3>${showToggle('showTwoWorlds')}${input('twoWorldsTitle','Nagłówek',true)}${input('twoWorldsDesc','Opis',true)}<div class="adminGrid" style="margin-top:14px"><div><h3>Fotografia</h3>${input('photoTitle','Tytuł')}${input('photoDesc','Opis',true)}${input('photoButton','Tekst przycisku')}<div id="hmPhotoMount"></div></div><div><h3>Film</h3>${input('filmTitle','Tytuł')}${input('filmDesc','Opis',true)}${input('filmButton','Tekst przycisku')}<div id="hmFilmMount"></div></div></div></div>
<div class="adminCard"><div class="eyebrow">03 / O MNIE</div><h3>Sekcja o Tobie</h3>${showToggle('showAbout')}${input('aboutEyebrow','Mały nagłówek')}${input('aboutTitle','Główny nagłówek',true)}${input('aboutDesc','Opis',true)}<div id="hmAboutMount"></div><div class="adminGrid" style="margin-top:14px"><div>${input('fact1Title','Kafelek 1 — tytuł')}${input('fact1Text','Kafelek 1 — opis')}</div><div>${input('fact2Title','Kafelek 2 — tytuł')}${input('fact2Text','Kafelek 2 — opis')}</div><div>${input('fact3Title','Kafelek 3 — tytuł')}${input('fact3Text','Kafelek 3 — opis')}</div></div></div>
<div class="adminGrid"><div class="adminCard"><div class="eyebrow">04 / OPINIE</div>${showToggle('showReviews')}${input('reviewsTitle','Nagłówek')}${input('reviewsDesc','Opis')}</div><div class="adminCard"><div class="eyebrow">05 / MARKI</div>${showToggle('showBrands')}${input('brandsTitle','Nagłówek')}${input('brandsDesc','Opis')}</div></div>
<div class="adminCard"><div class="eyebrow">06 / KONTAKT — TREŚĆ</div>${showToggle('showContact')}${input('contactEyebrow','Mały nagłówek')}${input('contactTitle','Nagłówek',true)}${input('contactDesc','Opis',true)}</div>
<div class="toolbar" style="margin:18px 0 28px"><button class="btn primary" id="hcSave">Zapisz całą stronę główną</button><span class="tiny" id="hcStatus"></span></div>`}
function fill(){document.querySelectorAll('#homeSectionsMount [data-k]').forEach(el=>el.value=state[el.dataset.k]??'');document.querySelectorAll('#homeSectionsMount [data-bool]').forEach(el=>el.checked=state[el.dataset.bool]!==false)}
function read(){document.querySelectorAll('#homeSectionsMount [data-k]').forEach(el=>state[el.dataset.k]=el.value);document.querySelectorAll('#homeSectionsMount [data-bool]').forEach(el=>state[el.dataset.bool]=el.checked)}
async function saveEverything(){read();const st=$('#hcStatus');st.textContent='Zapisywanie…';try{const siteSnap=await get(ref(db,`${WEBSITE_ROOT}/public/site`));const site={...(siteSnap.val()||{})};site.heroK=$('#heroK')?.value||'';site.heroT=$('#heroT')?.value||'';site.heroD=$('#heroD')?.value||'';await update(ref(db,`${WEBSITE_ROOT}/public`),{homeContent:state,site});const verify=await get(ref(db,`${WEBSITE_ROOT}/public/homeContent`));if(!verify.exists())throw new Error('Firebase nie potwierdził zapisu');st.textContent='Zapisano i potwierdzono w Firebase ✓'}catch(e){st.textContent='Błąd zapisu: '+e.message}}
async function init(){inject();const s=await get(ref(db,`${WEBSITE_ROOT}/public/homeContent`));state={...defaults,...(s.val()||{})};fill();$('#hcSave').onclick=saveEverything}
init();